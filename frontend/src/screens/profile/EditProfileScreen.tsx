import { useEffect, useState } from 'react'; import { Alert } from 'react-native'; import { router } from 'expo-router';
import { AuthLayout } from '@/screens/auth/AuthLayout'; import { FormField } from '@/components/auth/FormField'; import { PrimaryButton } from '@/components/auth/PrimaryButton'; import { FeedbackBanner } from '@/components/auth/FeedbackBanner'; import { profileService } from '@/services/profileService';
export default function EditProfileScreen() { const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [phone, setPhone] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); useEffect(() => { profileService.getProfile().then((p) => { setName(p.name); setEmail(p.email); setPhone(p.phone ?? ''); setLoading(false); }); }, []); const save = async () => { if (name.trim().length < 2) return setError('Please enter your name.'); if (!/^\S+@\S+\.\S+$/.test(email)) return setError('Please enter a valid email address.'); setSaving(true); setError(''); try { await profileService.updateProfile({ name: name.trim(), email: email.trim(), phone: phone.trim() }); Alert.alert('Saved', 'Your personal information has been updated.', [{ text: 'Done', onPress: () => router.back() }]); } catch { setError('We could not save your changes. Please try again.'); } finally { setSaving(false); } }; return <AuthLayout title="Personal information" subtitle="Keep your details up to date."><FeedbackBanner message={error}/><FormField label="Full name" value={name} onChangeText={setName} placeholder="Your name" autoComplete="name" editable={!loading}/><FormField label="Email address" value={email} onChangeText={setEmail} placeholder="you@example.com" keyboardType="email-address" autoCapitalize="none" editable={!loading}/><FormField label="Phone number (optional)" value={phone} onChangeText={setPhone} placeholder="+94 77 123 4567" keyboardType="phone-pad" editable={!loading}/><PrimaryButton title="Save changes" onPress={save} loading={saving || loading}/></AuthLayout>; }
