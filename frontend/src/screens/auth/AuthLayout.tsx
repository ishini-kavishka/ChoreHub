import { ReactNode } from 'react';
import { KeyboardAvoidingView, Platform, ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
export function AuthLayout({ eyebrow, title, subtitle, children }: { eyebrow?: string; title: string; subtitle: string; children: ReactNode }) {
  return <SafeAreaView style={styles.safe}><KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : undefined} style={styles.flex}><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><View style={styles.brand}><View style={styles.mark}><Text style={styles.markText}>C</Text></View><Text style={styles.brandText}>ChoreHub</Text></View>{eyebrow ? <Text style={styles.eyebrow}>{eyebrow}</Text> : null}<Text style={styles.title}>{title}</Text><Text style={styles.subtitle}>{subtitle}</Text><View style={styles.form}>{children}</View></ScrollView></KeyboardAvoidingView></SafeAreaView>;
}
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#FFF' }, flex: { flex: 1 }, content: { flexGrow: 1, padding: 24, paddingTop: 42 }, brand: { flexDirection: 'row', alignItems: 'center', gap: 10, marginBottom: 46 }, mark: { width: 36, height: 36, borderRadius: 12, backgroundColor: '#247B6B', alignItems: 'center', justifyContent: 'center' }, markText: { color: '#FFF', fontSize: 20, fontWeight: '900' }, brandText: { color: '#163B35', fontSize: 21, fontWeight: '800' }, eyebrow: { color: '#247B6B', fontSize: 13, fontWeight: '800', letterSpacing: .8, textTransform: 'uppercase', marginBottom: 10 }, title: { color: '#172B3A', fontSize: 31, fontWeight: '800', letterSpacing: -.5 }, subtitle: { color: '#657384', fontSize: 16, lineHeight: 23, marginTop: 10 }, form: { gap: 17, marginTop: 34 } });
