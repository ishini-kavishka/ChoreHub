import { useState } from 'react'; import { Text } from 'react-native'; import { router } from 'expo-router';
import { AuthLayout } from './AuthLayout'; import { FormField } from '@/components/auth/FormField'; import { PrimaryButton } from '@/components/auth/PrimaryButton'; import { FeedbackBanner } from '@/components/auth/FeedbackBanner'; import { authService } from '@/services/authService';
export default function ForgotPasswordScreen() { const [email, setEmail] = useState(''); const [message, setMessage] = useState(''); const [loading, setLoading] = useState(false); const submit = async () => { if (!/^\S+@\S+\.\S+$/.test(email)) return setMessage('Enter a valid email address.'); setLoading(true); try { await authService.requestPasswordReset(email); setMessage('If an account matches that address, reset instructions are on their way.'); } catch { setMessage('We could not send reset instructions. Please try again.'); } finally { setLoading(false); } }; return <AuthLayout title="Reset your password" subtitle="Enter your email and we’ll help you get back in."><FeedbackBanner message={message} type={message.startsWith('If ') ? 'success' : 'error'}/><FormField label="Email address" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" placeholder="you@example.com"/><PrimaryButton title="Send reset instructions" loading={loading} onPress={submit}/><Text style={{ color: '#247B6B', fontWeight: '800', textAlign: 'center' }} onPress={() => router.back()}>Back to sign in</Text></AuthLayout>; }
